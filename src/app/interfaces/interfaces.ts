export interface MenuItem {
    icon: string;
    title: string;
    width: number;
    heigth: number;
}

export interface CarouselItem {
    img: string;
    title: string;
    text: string;
}
